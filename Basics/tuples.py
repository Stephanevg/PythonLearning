mytuple = ("stephane","van Gulick","powershell","c++")
print(mytuple)

#tuples are just like lists. (Arrays in powershell actually)
#the only difference is that they are read only...

mytuple[3] = "phyton" #will throw error -> 'tuple' object does not support item assignment
mytuple